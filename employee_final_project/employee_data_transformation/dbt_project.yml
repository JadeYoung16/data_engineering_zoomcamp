name: 'employee_data_transformation'
version: '1.0.0'
config-version: 2

profile: 'employee_data_transformation'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  employee_data_transformation:
    staging:
      +materialized: view
    marts:
      +materialized: view